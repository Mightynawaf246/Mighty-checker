# Build output
igcheck
mighty
mighty.exe
instagram-username-checker
mighty-checker
*.exe

# Self-update leftovers
*.old
*.new

# Result files written on every run
available.txt
taken.txt
unknown.txt
errors.txt

# Your real lists. NEVER commit these - proxies.txt holds paid credentials.
# The tool creates them from the .example templates on first run.
username.txt
proxies.txt

# Credentials and claimer state. NEVER commit these - they are full accounts
# and live tokens in plain text. Copy from the .example templates and fill in.
session.txt
sessions.txt
claimed.json
claim.log
caught/
ids.txt

# Test binaries from -cpuprofile runs
*.test
